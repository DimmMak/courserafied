# 📚 QUOTES — Vanderbilt Prompt Engineering

> Auto-populated by `/courserafied ingest`. Schema in `schemas/QUOTES.template.md`.

---

## On the root prompt as seed (M1 L3)

> "You can think of the root prompt as providing a seed to the whole conversation — that seed will influence everything that comes later because it provides basically the rules that must be followed."

— Jules White, M1 L3

**Why memorable:** Frames root prompts not as one-shot instructions but as the root of a behavioral tree. Matches the `tree_structure_always` principle — one seed → every downstream response.

---

## On strength-of-rule language (M1 L3)

> "Typically when you're going to build one of these root prompts, you're going to say things like 'from now on forever, you will never do these things, you will always do these things.'"

— Jules White, M1 L3

**Why memorable:** The vocabulary of strong rules — "from now on forever," "never," "always" — is what makes a root prompt stick vs drift.

---

## On the hidden layer (M1 L3)

> "In many cases there's actually a root prompt that's hidden from the user and some of the different tools that you're going to end up working with."

— Jules White, M1 L3

**Why memorable:** Every tool you use daily (ChatGPT, Bing, Bard) has a root prompt you've never read. Its behavior is shaped by rules you can't see. Building your own tools = writing your own root prompt.

---

## On rules vs capabilities (M1 L3, paraphrased)

> "We can use the root prompt to kind of pretend like we're resetting ChatGPT's training cutoff."

— Jules White, M1 L3

**Why memorable:** The cutoff isn't a capability — it's a rule. You can rewrite the rule.
